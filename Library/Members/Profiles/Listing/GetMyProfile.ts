// @generated by Cratis. Source: Members.Profiles.Listing.MemberProfile. Time: 2026-06-08T07:35:55.9037130Z. Hash: 36729B7D2F6B00C7157EB1BB883AB7ED3A6A589679AB096BB2F1A8F607158980
/*---------------------------------------------------------------------------------------------
 *  **DO NOT EDIT** - This file is an automatically generated file.
 *--------------------------------------------------------------------------------------------*/

/* eslint-disable sort-imports */
// eslint-disable-next-line header/header
import { ObservableQueryFor, QueryResultWithState } from '@cratis/arc/queries';
import { useObservableQuery, useSuspenseObservableQuery, ObservableQueryWhen } from '@cratis/arc.react/queries';
import { ParameterDescriptor } from '@cratis/arc/reflection';
import { MemberProfile } from './MemberProfile';


/**
 * Observes the profile of the currently authenticated member, pushing updates whenever it changes. The member is resolved server-side from the authenticated identity &#8212; the client never supplies the identifier.
 */
export class GetMyProfile extends ObservableQueryFor<MemberProfile> {
    readonly route: string = '/api/profiles/listing/get-my-profile';
    readonly queryName: string = 'Members.Profiles.Listing.MemberProfile.GetMyProfile';
    readonly treatWarningsAsErrors: boolean = false;
    readonly roles: string[] = [];
    readonly defaultValue: MemberProfile = {} as any;

    constructor() {
        super(MemberProfile, false);
    }

    get requiredRequestParameters(): string[] {
        return [
        ];
    }

    readonly parameterDescriptors: ParameterDescriptor[] = [
    ];



    static use(): [QueryResultWithState<MemberProfile>] {
        const [result] = useObservableQuery<MemberProfile, GetMyProfile>(GetMyProfile);
        return [result];
    }

    static useSuspense(): [QueryResultWithState<MemberProfile>] {
        const [result] = useSuspenseObservableQuery<MemberProfile, GetMyProfile>(GetMyProfile);
        return [result];
    }

    static when(condition: boolean): ObservableQueryWhen<GetMyProfile, MemberProfile> {
        return new ObservableQueryWhen<GetMyProfile, MemberProfile>(GetMyProfile, condition);
    }
}
